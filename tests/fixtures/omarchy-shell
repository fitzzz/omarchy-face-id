#!/usr/bin/env bash

set -euo pipefail

[[ ${1:-} == "shell" ]]
[[ ${2:-} == "rescanPlugins" ]]

# Omarchy may time out while the watched plugin directory is already causing a
# reload. Activation must still be attempted after this non-zero result.
exit 1
