# TODO

- [ ] Require the user's eyes to be open and looking directly at the webcam during enrollment and authentication. The current implementation does not reliably enforce either condition. Add explicit detection thresholds, clear failure states, and regression tests without weakening the existing liveness checks.
